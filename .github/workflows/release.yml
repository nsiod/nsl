name: Release

# Builds per-platform binaries and publishes them as GitHub Release assets.
# Triggered by pushing a tag like `v0.1.0` or manually via workflow_dispatch.
#
# Packaging:
#   Unix:    nsl-<subpkg>.tar.gz    (contains ./nsl, client)
#            nsld-<subpkg>.tar.gz   (contains ./nsld, server daemon)
#   Windows: nsl-<subpkg>.zip       (contains nsl.exe)
#            nsld-<subpkg>.zip      (contains nsld.exe)
#
# Docker images for nsld are built and pushed to Docker Hub by the
# separate `docker.yml` workflow (also tag-triggered).
#
# Downstream: run the manual `npm` workflow with this tag to publish npm
# platform subpackages from these release assets (npm only consumes the
# `nsl-*` archives, ignores `nsld-*`).
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version tag to create (e.g. v0.1.0)"
        required: true

env:
  CARGO_TERM_COLOR: always

jobs:
  build-linux:
    name: build linux (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            subpkg: linux-x64
          - target: aarch64-unknown-linux-musl
            os: ubuntu-24.04-arm
            subpkg: linux-arm64
    steps:
      - uses: actions/checkout@v6

      - name: Install musl toolchain
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Install Rust target
        run: rustup target add ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build release
        run: cargo build --release --target ${{ matrix.target }} --locked

      - name: Package tarballs
        run: |
          set -eu
          mkdir -p dist
          for bin in nsl nsld; do
            cp "target/${{ matrix.target }}/release/${bin}" "${bin}"
            chmod +x "${bin}"
            tar -czf "dist/${bin}-${{ matrix.subpkg }}.tar.gz" "${bin}"
            rm "${bin}"
          done

      - uses: actions/upload-artifact@v7
        with:
          name: asset-${{ matrix.subpkg }}
          path: dist/*.tar.gz
          if-no-files-found: error
          retention-days: 7

  build-macos:
    name: build macos (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-apple-darwin
            os: macos-26-intel
            subpkg: darwin-x64
          - target: aarch64-apple-darwin
            os: macos-26
            subpkg: darwin-arm64
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust target
        run: rustup target add ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build release
        run: cargo build --release --target ${{ matrix.target }} --locked

      - name: Package tarballs
        run: |
          set -eu
          mkdir -p dist
          for bin in nsl nsld; do
            cp "target/${{ matrix.target }}/release/${bin}" "${bin}"
            chmod +x "${bin}"
            tar -czf "dist/${bin}-${{ matrix.subpkg }}.tar.gz" "${bin}"
            rm "${bin}"
          done

      - uses: actions/upload-artifact@v7
        with:
          name: asset-${{ matrix.subpkg }}
          path: dist/*.tar.gz
          if-no-files-found: error
          retention-days: 7

  build-windows:
    name: build windows (${{ matrix.target }})
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-pc-windows-msvc
            subpkg: win32-x64
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust target
        run: rustup target add ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build release
        run: cargo build --release --target ${{ matrix.target }} --locked

      - name: Package zips
        shell: bash
        run: |
          set -eu
          mkdir -p dist
          for bin in nsl nsld; do
            cp "target/${{ matrix.target }}/release/${bin}.exe" "${bin}.exe"
            7z a "dist/${bin}-${{ matrix.subpkg }}.zip" "${bin}.exe"
            rm "${bin}.exe"
          done

      - uses: actions/upload-artifact@v7
        with:
          name: asset-${{ matrix.subpkg }}
          path: dist/*.zip
          if-no-files-found: error
          retention-days: 7

  release:
    name: publish github release
    needs: [build-linux, build-macos, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - name: Resolve tag name
        id: tag
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            echo "name=${{ github.event.inputs.version }}" >> "$GITHUB_OUTPUT"
          else
            echo "name=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          fi

      - name: Download all platform assets
        uses: actions/download-artifact@v8
        with:
          pattern: asset-*
          path: artifacts
          merge-multiple: true

      - name: List assets
        run: ls -la artifacts/

      - name: Create tag (if dispatched)
        if: github.event_name == 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git tag "${{ steps.tag.outputs.name }}"
          git push origin "${{ steps.tag.outputs.name }}"

      - name: Create GitHub Release with assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Both `nsl-*` (client) and `nsld-*` (server) archives ship.
          gh release create "${{ steps.tag.outputs.name }}" \
            --title "${{ steps.tag.outputs.name }}" \
            --generate-notes \
            artifacts/*.tar.gz artifacts/*.zip
